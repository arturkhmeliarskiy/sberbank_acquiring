import 'package:flutter/material.dart';

class DowlandSuccessful extends StatelessWidget {
  const DowlandSuccessful({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Скачано'),
      ),
    );
  }
}
